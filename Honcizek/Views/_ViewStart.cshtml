@{
    if (Context.Request.Path != "/" && ViewData["layout"] != "")
    {
        string s = Context.Request.Path;
        var area = s.Split("/");
        Layout = area[1];
    }
    @*else
    Layout = (string)ViewData["Layout"] ?? "_Admin";*@
}
